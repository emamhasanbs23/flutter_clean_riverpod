{
  "_comment": "Dev environment. Copy env.example.json to env.dev.json and adjust. The flavor defaults already point at the dev backend; this file is here so a developer can override without touching code.",

  "BASE_URL": "https://dev.api.example.com",
  "APP_NAME": "Boilerplate (Dev)",
  "DIO_LOGGING": "true"
}
