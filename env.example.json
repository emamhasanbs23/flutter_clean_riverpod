{
  "_comment": "Template for env.dev.json / env.staging.json / env.prod.json. Copy to env.<flavor>.json and fill in real values. The .env.* files are gitignored.",

  "BASE_URL": "https://dev.api.example.com",
  "APP_NAME": "Boilerplate (Dev)",
  "DIO_LOGGING": "true"
}
